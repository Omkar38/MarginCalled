The agent logged 8,508 decisions this session, all on SPX and all in the `european_native` theory category. Five reached execution. The remaining 8,503 were refused: 6,845 (80.5%) at the tradability stage and 1,658 (19.5%) at the risk stage.

The single largest cause of refusal, by a wide margin, is the tradability coverage-ratio gate. Every `not_executable` record in the sample was refused for the same reason — "coverage_ratio too wide" — with values ranging from 1.255 up to 37.13. The extreme case (SPX261218C11200000, ratio 37.13) also flagged a cheapest leg of $0.12, i.e. a deep-OTM rectangle whose detected edge was 1.98 against near-worthless quotes. The typical refusal in this group is much tamer: a ratio between 1.25 and 1.9 on legs priced in the hundreds or thousands, where the violation is real on paper but small relative to the width being crossed.

Risk-stage refusals fall into four causes, frequently stacked in the same record:

- **Position size against the loss caps.** The most common single code is `max_loss_per_trade` — max loss above the $2,500 per-trade cap (2.5% of equity) — often accompanied by `max_aggregate_loss` above the $10,000 cap. Sampled figures run from $3,046 to $62,974, i.e. an order of magnitude over the cap in many cases.
- **The violation disappearing on revalidation.** `violation_gone` appears repeatedly, either because the fresh violation size came back negative (e.g. −186,269 on SPX270521C05650000) or because the rectangle simply no longer violated on fresh quotes. In several of these the sizing was fine — SPX270521C06775000 passed the loss caps at $450 max loss and was refused purely because the edge had gone.
- **Decay below the retention floor.** `violation_decayed` refused candidates that retained only 11.9% and 8.4% of the detected violation against a 50% minimum.
- **The daily entry cutoff.** A cluster of candidates timestamped 15:57 were all refused by `after_daily_cutoff` (cutoff 15:55). Two of those (SPX261120C06375000, SPX261016C07330000) had passed every other check, including the loss caps, and were refused on timing alone.

The five traded decisions were all single-quantity, two-leg T1 packages with max losses between $327 and $1,920, comfortably inside the $2,500 cap, and all with zero positions already open. Four are recorded as dry runs not sent to the broker; one (SPX261120C06950000, decision a9400c157b9f) was submitted via MCP with order id db85bfe6-7354-4226-8821-5891c72ccefa. No fill outcomes are present in the records for any of them.

Two things are worth a human's attention. First, on four of the five traded orders the price shade was clamped to 50% of the net because a full package-spread shade would have inverted the sign of the order — the quoted spreads are wide relative to the edge being claimed, and the notes state plainly that a fill requires the real NBBO to be better than the indicative quote implied. Second, the one order actually submitted carries two conflicting revalidation entries in its checks list: "100.0% of detected violation retained" and "305.1% of detected violation retained", with `fresh_violation_size` 13,165 against a detected 4,315. That is the only record in the sample where the retained fraction exceeds 1.0, and it is the only live submission. Separately, the 15:57 batch shows the scanner producing candidates after the entry window has closed, which the cutoff gate absorbs but which represents work that can never convert.
