Across the session the agent logged 5,335 decisions on SPY rectangles. Three reached execution; 5,332 were refused. The record set shown here is a 105-row sample, so the counts below come from the outcome totals and the causes from the sample.

**Traded (3).** All three fired at 14:30:00 on the first scan, all in the `no_distribution` theory category, all T1-denominated, all sized at qty 1: SPY260904C00520000 (violation 824.36, max loss $130), SPY260903C00650000 (violation 128.93, max loss $1,257) and SPY260903C00733000 (violation 0.89, max loss $94). Each passed the full risk list — kill switch, daily loss, entry window, quote age ~0.2s, 100% of the violation retained on revalidation, per-trade and aggregate loss caps, position count and duplicate-leg check. All three were dry-run: `broker.status` is `dry_run` and no order ID exists, so no fills or P&L are recorded. Each limit was shaded by the full package spread (0.36, 0.72, 0.25), which the order notes describe as requiring the real NBBO to be better than the indicative quote implied.

**Not executable (4,261, ~80%).** Refused at the tradability stage before theory or risk. Two reasons, usually together: a coverage ratio judged too wide, and a cheapest leg below the price floor. The ratios in the sample range from 1.11 to 34.6. These split into two distinct populations — deep-OTM rectangles where every leg is a 1–5 cent quote (e.g. SPY260930C00890000, ratio 34.6, cheapest leg $0.03) and deep-ITM rectangles with 200-point legs but wide books (e.g. SPY270617C00540000, ratio 1.51, cheapest leg mid $242.65). A few were refused on the cheap-leg test alone despite an acceptable ratio (SPY260918C00801000, ratio 1.11, cheapest leg $0.08).

**Theory blocked (1,047, ~20%).** Every theory-gate refusal in the sample gives the same reason: T2 extends past the dividend horizon 2026-09-21, no distribution is announced that far out but one is expected, so the absence of early exercise cannot be asserted. All were categorised `unresolved`. The blocked T2s run from 2026-09-30 out to 2027-09-17.

**Risk rejected (24).** Three causes. Most sample rejections are `violation_gone` — on refresh the violation had inverted, sometimes heavily (SPY260904C00515000 detected at +641.75, refreshed at -3,573.28). Several are `max_loss_per_trade`, where sizing produced a max loss above the $2,500 cap (2.50% of equity), up to $4,825. Four at 15:57 are `after_daily_cutoff`: past the 15:55 entry cutoff, with everything else passing.

**Pattern worth attention.** Two things. First, the `violation_gone` cluster is concentrated in time — the 15:43:33–15:51:41 scans produced a run of them, with detected violations of a few hundred flipping to negative several hundred or thousand on a refresh taken 0.1–0.4 seconds later. Second, and more concretely, four of those records (db047a2431bb, 96756c7e408d, d0b84f4017a6, 3df172845451) list "revalidated: 100.0% of detected violation retained" in `checks_passed` and carry `violation_retained: 1.0`, while simultaneously reporting a `violation_gone` rejection with a negative fresh size. The two fields contradict each other in the same record; the other `violation_gone` cases have `violation_retained: null` and no revalidation line. The records do not explain the discrepancy.
